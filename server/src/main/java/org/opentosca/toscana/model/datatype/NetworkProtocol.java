package org.opentosca.toscana.model.datatype;

/**
 Used to express a valid network protocol
 (TOSCA Simple Profile in YAML Version 1.1, p. 153)
 */
public enum NetworkProtocol {
    HTTP,
    HTTPS,
    FTP,
    TCP,
    UDP
}
