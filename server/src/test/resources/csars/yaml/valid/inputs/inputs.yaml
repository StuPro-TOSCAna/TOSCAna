tosca_definitions_version: tosca_simple_yaml_1_1
description: Template which only contains inputs
metadata:
  template_name: inputs
  template_author: stupro-toscana
  template_version: 1.0

topology_template:
  inputs:
    string-input:
      type: string
      description: description1
      required: true
    boolean-input:
      type: boolean
      description: description2
    integer-input:
      description: description3
      type: integer
      required: false
    float-input:
      description: description4
      type: float
      required: false
      default: default-value
