tosca_definitions_version: tosca_simple_yaml_1_1

description: template for testing detection whether inputs are set/ not set. Outcome for this template shall be 'not set'.
metadata:
  template_name: input_no-value
  template_author: stupro-toscana
  template_version: 1.0

topology_template:
  inputs:
    test_input:
      type: string
      description: test-description
      required: true
