tosca_definitions_version: tosca_simple_yaml_1_1

description: a template containing a single, valid software component
metadata:
  template_name: empty-topology
  template_author: stupro-toscana
  template_version: 1.0

topology_template:
  node_templates:
    software_component:
      type: SoftwareComponent
      properties:
        component_version: 3.5.1
        admin_credential:
          token: securePassword
          user: alice

